#include <string.h>
#include <unistd.h>
#include "libft.h"

#define BUFFER 128

int	main()
{
	void	*b1;
	void	*b2;

	b1 = malloc(BUFFER);
	b2 = malloc(BUFFER);
	if (!b1 || !b2)
		return 0;

	
	ft_split()
}
